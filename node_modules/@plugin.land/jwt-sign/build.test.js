
var a = require('@plugin.land/run-process')
var b = require('@plugin.land/log-state')
var c = require('@plugin.land/access-context')
var d = require('@plugin.land/generate-ssl-certificate')
var plugin = require('./')

module.exports = function(build) {
  build.use(a)
  build.use(b)
  build.use(c)
  build.use(d)
  build.use(plugin)
}
