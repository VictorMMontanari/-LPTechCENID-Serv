
var plugin = require('./')
var MongoDBInitialize = require('@plugin.land/mongodb-initialize')

module.exports = function(build) {
  build.plugin('MongoDB persist state', plugin)
  build.use(MongoDBInitialize)
}
