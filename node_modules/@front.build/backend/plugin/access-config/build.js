
var plugin = require('./')
var configlandaccessconfig = require('@plugin.land/config.land-access-config')

module.exports = function(build) {
  build.plugin('Access config', plugin)
  build.use(configlandaccessconfig)
}
