
/**
 */

module.exports = function(state, action, callback) {
  state.dispatch({
    type: 'Config.land access config',
    input: {
      Properties: action.input.Properties,
      'Property name': action.input['Property name']
    }
  }, callback)
}
