
/**
 * Use this to access like a REST API the data from this service.
 */

module.exports = function(state, action, callback) {
  state.dispatch({
    type: 'Request JSON',
    input: {
      URL: 'https://field.black',
      Method: 'POST',
      Body: action.input.Action
    }
  }, callback)
}
