
var Stripe = require('stripe')

module.exports = function(state, action, callback) {
  var db = state.db
  var stripeSecret = action.input['Stripe secret']
  var stripe = new Stripe(stripeSecret)
  stripe.charges.list({}, function(err, charges){
    callback(charges)
  })
}
