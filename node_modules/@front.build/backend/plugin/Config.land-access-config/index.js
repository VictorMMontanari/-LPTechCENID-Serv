
module.exports = function(state, action, callback) {
  state.dispatch({
    type: 'Request JSON',
    input: {
      URL: 'https://config.land',
      Method: 'POST',
      Body: JSON.stringify({
        type: 'Access config',
        input: action.input
      })
    }
  }, callback)
}
