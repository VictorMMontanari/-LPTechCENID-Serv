
module.exports = State

function State(vars) {
  this.vars = vars
}

State.prototype.merge = function(object) {
  for (var name in this.vars) {
    object[name] = this.vars[name]
  }
  return object
}
